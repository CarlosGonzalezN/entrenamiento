

    const arrFuntion = (ar1)=>{
        let i = ar1.length
        let index = 0
        for(index; i > index ;index++) {
            if(ar1[index]=== 'banana'){
                console.log(ar1[index], 'posision :');
            }
            
        }
    }
module.exports= {
    arrFuntion
}